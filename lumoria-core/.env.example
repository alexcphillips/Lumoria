# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# Application Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=lumoria_user
DB_PASSWORD=lumoria_password
DB_DATABASE=lumoria_db

# Alternative for SQLite (for development)
# DB_TYPE=sqlite
# DB_DATABASE=./lumoria.sqlite